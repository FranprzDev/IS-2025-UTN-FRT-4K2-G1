{
  "name": "4K2-G11-2025-LaChangada",
  "version": "1.1.0",
  "description": "Clínica de Emergencia 2025 - Proyecto Final Integrador Backend hecho con BDD con Typescript",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test:cucumber_unique": "tsx node_modules/@cucumber/cucumber/bin/cucumber.js test/features/moduloUrgencias.feature --import test/features/step_definitions/modulo_urgencias.step.ts",
    "test:cucumber": "tsx node_modules/@cucumber/cucumber/bin/cucumber.js test/features/*.feature --import test/features/step_definitions/*.ts",
    "test:unit": "tsx --test test/unit/**/*.test.ts",
    "test:unit_file": "tsx --test",
    "test": "npm run test:unit && npm run test:cucumber",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "dev": "tsx server.ts",
    "build": "tsc",
    "kill-ports": "node -e \"const { execSync } = require('child_process'); const puertos = [3000, 3001]; const obtenerPids = (linea) => { const partes = linea.trim().split(/\\s+/); return partes[partes.length - 1]; }; puertos.forEach((puerto) => { try { const salida = execSync('netstat -ano | findstr :' + puerto, { encoding: 'utf8' }); const lineas = salida.split(/\\r?\\n/).filter(Boolean); const pids = [...new Set(lineas.map(obtenerPids).filter(Boolean))]; if (pids.length === 0) { console.log('Puerto', puerto, 'libre'); return; } pids.forEach((pid) => { try { execSync('taskkill /PID ' + pid + ' /F'); console.log('Finalizado PID', pid, 'en puerto', puerto); } catch (error) { console.error('No se pudo terminar PID', pid, 'en puerto', puerto, '-', error.message); } }); } catch { console.log('Puerto', puerto, 'libre'); } });\""
  },
  "keywords": [],
  "author": "4K2-G11-2025-LaChangada",
  "license": "ISC",
  "dependencies": {
    "@types/ms": "^2.1.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "cross-env": "^10.1.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/chai": "^5.2.2",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^24.7.2",
    "chai": "^6.2.0",
    "eslint": "^9.37.0",
    "prettier": "3.6.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
